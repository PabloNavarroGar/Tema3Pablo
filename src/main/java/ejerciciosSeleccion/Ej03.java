/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package ejerciciosSeleccion;

/**
 *
 * @author samue
 */
public class Ej03 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here

        //a 
        /*boolean unaCondicion = true;
        resultado = unaCondicion ? valor1 : valor2;
        // Significa qe si el resultado de la condicion es valor1 sera true y si es 2 sera falso(valor2
        //b
        int x = 10;
        int y = (x > 9) ? 100 : 200;

        System.out.println(" El resultado es " + y);
        // Quiere decir que si x vale 10, en la experion ternaria si 10 es mayor que nueve, dara 100, si no lo es dara 200

        //c
        int publico = 19500;
        int vendidas = 19000;
        int aforo = 20000;
        //Errata por parte del ejercicio
        String status = (publico < aforo) ? "Cabe mas gente" : (vendidas < aforo) ? "Aun no hemos vendido todo" : "Esta todo vendido";

        System.out.println(status);

         /*
        En este operador ternario quiere decir que en dado si hay menos gente(publico) que el aforo, cabe mas gente
        luego  si se da la posibilidad de que si las entradas vendias hay menos que el aforo, aun no no se vendieron todas, y la otra posibilidad es que esta todo vendido
        */
        
        
    }
        

}
